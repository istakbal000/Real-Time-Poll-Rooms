{
  "name": "real-time-poll-rooms",
  "version": "1.0.0",
  "description": "Real-time polling application with React and Node.js",
  "scripts": {
    "start": "cd server && npm start",
    "build": "echo 'STARTING BUILD...' && npm install --prefix server && npm install --prefix client --include=dev && npm run build --prefix client && echo 'BUILD COMPLETE - CHECKING DIST:' && ls -la client/dist",
    "postinstall": "npm run build",
    "heroku-postbuild": "npm run build"
  },
  "keywords": [
    "react",
    "nodejs",
    "socket.io",
    "mongodb",
    "polling",
    "real-time"
  ],
  "author": "istakbal000",
  "license": "ISC"
}